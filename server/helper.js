
const findEnvById = (id) => {
    for(let i = 0; i < envelopes.length; i++) {
        if (envelopes[i].id = id) {
            return envelopes[i]; 
        }
    }
}
const findBudById = (id) => {
    for(let i = 0; i < envelopes.length; i++) {
        if (envelopes[i].id = id) {
            return envelopes[i].budget; 
         }
    }
}




